/**
 * @typedef Options
 * @type {object}
 */
import van from '../van.min.js';

const { div } = van.tags;
const { circle, svg, text } = van.tags("http://www.w3.org/2000/svg");

const Spinner = () => {

};

export { Spinner };
